const { upsertJobs } = require('../upsertJobs');

const syncMantiks = async () => {
  console.log('[sync:mantiks] Stub — implement fetch and normalize, then call upsertJobs');
};

module.exports = { syncMantiks };